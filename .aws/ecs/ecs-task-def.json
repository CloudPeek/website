{
  "family": "cloudpeek-website",
  "containerDefinitions": [
    {
      "name": "cloudpeek",  // Update this to match the workflow
      "image": "cloudpeek/website:latest",
      "essential": true,
      "memory": 512,
      "cpu": 256,
      "portMappings": [
        {
          "containerPort": 3000,
          "hostPort": 3000
        }
      ]
    }
  ]
}
